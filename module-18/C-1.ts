/* 🔧 ALTER কী?

ALTER ব্যবহার করা হয় ডাটাবেসে আগে থেকে থাকা কোনো জিনিস পরিবর্তন করতে।

👉 যেমন:

table এর গঠন বদলানো

column যোগ / বাদ দেওয়া

column এর type পরিবর্তন

constraint যোগ / বাদ

table / column এর নাম বদলানো */

/**
/**
============================================================
 PostgreSQL ALTER TABLE – Beginner Friendly Notes (Bangla)
============================================================
*/

/*
------------------------------------------------------------
1) ADD A COLUMN (নতুন column যোগ করা)
------------------------------------------------------------
কখন ব্যবহার করবো?
- যখন টেবিলে নতুন তথ্য রাখার প্রয়োজন হয়
- যেমন: phone, address, status ইত্যাদি
*/

// Basic example
// users টেবিলে phone নামে নতুন column যোগ করা
`ALTER TABLE users
 ADD COLUMN phone TEXT;`;
// Default value সহ column যোগ করা // Notes: // ================
`ALTER TABLE users
ADD COLUMN status TEXT DEFAULT 'active';`; /*
Important Notes:
- পুরোনো row গুলোর জন্য DEFAULT value বসে যাবে
- DEFAULT না দিলে পুরোনো row গুলোতে NULL থাকবে
*/ /*
------------------------------------------------------------
2) DROP A COLUMN (column মুছে ফেলা)
------------------------------------------------------------
কখন ব্যবহার করবো?
- কোনো column আর দরকার নেই
- ভুল করে বানানো হয়েছে
*/ // age column পুরোপুরি মুছে ফেলা
`ALTER TABLE users
DROP COLUMN age;`; /*
WARNING ⚠️
- Column এর সব ডাটা চিরতরে মুছে যাবে
- Undo করা যাবে না
- Production এ খুব সাবধানে ব্যবহার করতে হবে
*/ /*
------------------------------------------------------------
3) RENAMING A COLUMN (column এর নাম পরিবর্তন)
------------------------------------------------------------
কখন ব্যবহার করবো?
- ভুল নাম দেওয়া হয়েছে
- আরো meaningful নাম দরকার
*/ // name column এর নাম full_name করা
`ALTER TABLE users
RENAME COLUMN name TO full_name;`; /*
Notes:
- ডাটার কোনো ক্ষতি হয় না
- শুধু column এর নাম পরিবর্তন হয়
*/
/**
 * ============================================================
 * 4) modifying type
 * ============================================================
 * -- modifying type
 * */
`ALTER TABLE users
  ALTER COLUMN name TYPE VARCHAR(50)`;
/*
 *
 * এখানে কি হচ্ছে:
 * 1. ALTER TABLE users → users টেবিলের ভিতর পরিবর্তন করা হবে।
 * 2. ALTER COLUMN name → name কলাম পরিবর্তনের জন্য নির্বাচন করা হচ্ছে।
 * 3. TYPE VARCHAR(50) → কলামের ডেটা টাইপকে VARCHAR(50) তে পরিবর্তন করা হচ্ছে।
 *
 * ============================================================
 */
