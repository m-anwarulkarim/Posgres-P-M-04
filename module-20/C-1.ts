// 🔥 USING কী?
// USING তখন ব্যবহার হয় যখন দুই টেবিলের column name একই থাকে
// এবং তুমি সেই column দিয়েই join করতে চাও।

// ---------------------------------------------------------
// 1️⃣ সাধারণ JOIN (ON দিয়ে)
// ---------------------------------------------------------
const joinWithOn = `
SELECT *
FROM users
INNER JOIN orders
ON users.id = orders.id;
`;

// ---------------------------------------------------------
// 2️⃣ USING দিয়ে Short & Clean JOIN
// ---------------------------------------------------------
const joinUsing = `
SELECT *
FROM users
INNER JOIN orders
USING (id);
`;

// ---------------------------------------------------------
// USING মানে PostgreSQL নিজেই বুঝে নেয়:
// users.id = orders.id
// ---------------------------------------------------------
const joinExplanation = `
-- USING মানে:
-- users.id = orders.id
`;

// ---------------------------------------------------------
// 3️⃣ USING এ Duplicate Column দেখাবে না
// দুই টেবিলে id থাকলেও result এ একটাই id থাকবে
// ---------------------------------------------------------
const joinNoDuplicate = `
SELECT
  id,       -- duplicate দেখাবে না
  name,
  amount
FROM users
INNER JOIN orders
USING (id);
`;

// ---------------------------------------------------------
// 4️⃣ USING কবে ব্যবহার করা যাবে না?
// যখন column নাম দুই টেবিলে একই না
// ---------------------------------------------------------
const usingNotAllowed = `
SELECT *
FROM users
INNER JOIN orders
ON users.id = orders.user_id;
`;

// ---------------------------------------------------------
// 🧠 Golden Rule
// USING => একই column নাম হলে
// ON    => column নাম আলাদা হলে বা complex join হলে
// ---------------------------------------------------------
const goldenRule = `
-- USING => দুই টেবিলেই column name একই হলে join করা যাবে
-- ON    => column name আলাদা হলে অথবা multiple condition লাগলে
`;
