// 1.LIMIT  2.OFFSET Pagination
/*
====================================================
🔹 Introduction to LIMIT & OFFSET Pagination
====================================================
ধরা যাক, আমাদের একটি বড় table আছে যেখানে হাজার হাজার row রয়েছে। কিন্তু আমরা চাই একসাথে সব ডেটা না এনে, নির্দিষ্ট সংখ্যক row আনা হোক।
/*
====================================================
🔹 1️⃣ LIMIT Pagination কী?
====================================================
LIMIT Pagination হলো এমন একটি পদ্ধতি যেখানে একসাথে সব ডেটা না এনে, নির্দিষ্ট সংখ্যক row আনা হয়।

উদাহরণ: ওয়েবসাইটে ১০টি পোস্ট এক পেজে দেখানো, পরের ১০টি পেজে “Next” ক্লিক করলে আসে।
*/

/*
====================================================
🔹 2️⃣ LIMIT Syntax
====================================================
*/
const limitSyntax = `
SELECT column1, column2
FROM table_name
ORDER BY column
LIMIT number_of_rows;
`;

/*
ব্যাখ্যা:
- LIMIT number_of_rows → কত row আনা হবে
- ORDER BY দেওয়া উচিত, না হলে row random হবে

Example:
*/
`SELECT * FROM users
ORDER BY id
LIMIT 5;
`;
/*
প্রথম ৫টা row আনা হবে
Row random না হতে ORDER BY ব্যবহার করা ভালো
*/

/*
====================================================
🔹 1️⃣ OFFSET কী?
====================================================
OFFSET মানে: কতটা row skip করতে হবে।
সাধারণত Pagination এর সাথে ব্যবহার হয়।

LIMIT ছাড়া OFFSET মানে কিছু row skip হবে কিন্তু কতটা আনা হবে বলা হয়নি → সব row আনা হবে
*/

/*
====================================================
🔹 2️⃣ OFFSET Syntax
====================================================
*/
const offsetSyntax = `
SELECT column1, column2
FROM table_name
ORDER BY column
OFFSET number_of_rows_to_skip;
`;

/*
ব্যাখ্যা:
- OFFSET optional, default = 0
- সাধারণত LIMIT এর সাথে ব্যবহার করা হয়
*/

/*
====================================================
🔹 3️⃣ OFFSET + LIMIT উদাহরণ
====================================================
ধরা যাক table users:

id | name
1  | Anwar
2  | Karim
3  | Rahim
4  | Hasan
5  | Akash
6  | Rafiq

Page 1 (প্রথম 2 row)
SELECT * FROM users
ORDER BY id
LIMIT 2 OFFSET 0;

Result:
1 | Anwar
2 | Karim

Page 2 (পরের 2 row)
*/
`SELECT * FROM users
ORDER BY id
LIMIT 2 OFFSET 2;`;
/*

Result:
3 | Rahim
4 | Hasan

Page 3 (পরের 2 row)
*/
`SELECT * FROM users
ORDER BY id
LIMIT 2 OFFSET 4`; /*

Result:
5 | Akash
6 | Rafiq
*/

/*
====================================================
🔹 4️⃣ OFFSET একা ব্যবহার করা
====================================================
SELECT * FROM users
ORDER BY id
OFFSET 3;

📌 মানে: প্রথম ৩টা row skip → বাকি সব row আনা হবে
Limit না দিলে সব remaining row আসে

Result:
4 | Hasan
5 | Akash
6 | Rafiq
*/

/*
====================================================
🔹 Pagination Real-life Example
====================================================

Page 1 (OFFSET = 5 * (1-1) = 0)
*/
`SELECT * FROM products
ORDER BY id
LIMIT 5
OFFSET 5 * 0`;
/*

📌 মানে: প্রথম ৫টা row দেখাবে, row skip = 0

Page 2 (OFFSET = 5 * (2-1) = 5)
*/
`SELECT * FROM products
ORDER BY id
LIMIT 5
OFFSET 5 * 1`;

// 📌 মানে: প্রথম ৫টা row skip করবে, পরের ৫টা row দেখাবে

`        -- 1ম page
        SELECT * FROM products ORDER BY id LIMIT 5 OFFSET 0;

        -- 2য় page
        SELECT * FROM products ORDER BY id LIMIT 5 OFFSET 5;

        -- 3য় page
        SELECT * FROM products ORDER BY id LIMIT 5 OFFSET 10;`;

// 📌 প্রতিটি page-এ LIMIT = কত row দেখাবে
// 📌 প্রতিটি page-এ OFFSET = কত row skip করবে
//
