/**
 * ====================================================
 * 🔑 Foreign Key কী? (Core Concept)
 * ====================================================
 *
 * Foreign Key হলো এমন একটি column (বা একাধিক columns)
 * 👉 যা অন্য একটি table-এর Primary Key (বা Unique Key) কে reference করে।
 *
 * Foreign Key-এর কাজ:
 * - দুইটা table-এর মধ্যে relationship তৈরি করা
 * - Data integrity বজায় রাখা
 * - ভুল বা ভাঙা data insert হওয়া থেকে আটকানো
 *
 * সাধারণত:
 * - Child Table  → Foreign Key থাকে
 * - Parent Table → Primary / Unique Key থাকে
 */

// ====================================================
// 🔹 Foreign Key কিভাবে কাজ করে?
// ====================================================

// উদাহরণ ধরা যাক:

// ----------------------
// 🟢 Parent Table
// ----------------------
const createUsersTable = `
CREATE TABLE users (
  id INT PRIMARY KEY,
  name VARCHAR(100)
);
`;

// ----------------------
// 🔵 Child Table
// ----------------------
const createOrdersTable = `
CREATE TABLE orders (
  order_id INT PRIMARY KEY,
  product VARCHAR(100),
  user_id INT REFERENCES users(id) -- Foreign Key
);
`;

/**
 * ব্যাখ্যা:
 * - orders টেবিলের user_id হলো Foreign Key
 * - এটি users টেবিলের id (Primary Key) কে reference করে
 * - অর্থাৎ, orders টেবিলে user_id তে এমন কোনো মান দেওয়া যাবে না
 *   যা users টেবিলে আগে থেকে নেই
 * - এতে করে data integrity বজায় থাকে
 * - ভুল বা অবৈধ data insert হওয়া থেকে রক্ষা পাওয়া যায়
 */

// ====================================================
// ✅ Foreign Key এর সুবিধা
// ====================================================

// Foreign Key এর সুবিধা:
// 1. Data Integrity বজায় রাখা
//  2. Table-এর মধ্যে সম্পর্ক তৈরি করা
//  3. Cascading Actions (যেমন: DELETE বা UPDATE)
// 4. Query Performance উন্নত করা
// 5. Referential Integrity নিশ্চিত করা
