/*
====================================================
🔹 INNER JOIN কী? (Core Concept)
====================================================

INNER JOIN হলো SQL-এর এমন একটি JOIN
👉 যেটা দুই (বা একাধিক) টেবিলের মধ্যে
মিল থাকা (matching) রেকর্ডগুলোই শুধু দেখায়।

📌 যেগুলোর মধ্যে মিল নাই — সেগুলো দেখায় না।
*/

/*
====================================================
🔹 কেন INNER JOIN দরকার?
====================================================

বাস্তবে ডেটাবেসে তথ্য সাধারণত আলাদা আলাদা টেবিলে থাকে।

উদাহরণ:
- users টেবিলে ইউজারের তথ্য
- orders টেবিলে অর্ডারের তথ্য

👉 কিন্তু আমরা চাই:
কোন ইউজার কোন অর্ডার করেছে — এই combined তথ্য

এই কাজটাই করে INNER JOIN।
*/

/*
====================================================
🔹 INNER JOIN এর Basic Syntax
====================================================
*/
`SELECT column_list
FROM table1
INNER JOIN table2
ON table1.column = table2.column`;
/*
🔹 ON → কোন কলামের মাধ্যমে দুইটা টেবিল যুক্ত হবে
🔹 মিল থাকলেই শুধু row আসবে
*/

/*
====================================================
🔹 বাস্তব উদাহরণ 
====================================================

👤 users টেবিল
--------------------------------
id | name
--------------------------------
1  | Rahim
2  | Karim
3  | Selim

📦 orders টেবিল
--------------------------------
id | user_id | product
--------------------------------
1  | 1       | Laptop
2  | 2       | Mobile
3  | 4       | Tablet
*/

// 🔹 INNER JOIN Query
// --------------------------------

`SELECT users.name, orders.product
FROM users
INNER JOIN orders
ON users.id = orders.user_id`;

/*
====================================================
🔹 Output কী আসবে?
====================================================

name   | product
-----------------
Rahim  | Laptop
Karim  | Mobile

❌ Selim আসেনি
❌ user_id = 4 আসেনি

👉 কারণ:
users.id = orders.user_id — এই মিলটাই INNER JOIN খোঁজে
*/

/*
====================================================
🔹 INNER JOIN কীভাবে কাজ করে? (Conceptually)
====================================================

1️⃣ প্রথম টেবিল নেয়
2️⃣ দ্বিতীয় টেবিলের সাথে ON condition মিলায়
3️⃣ যেগুলো match করে → দেখায়
4️⃣ যেগুলো match করে না → বাদ দেয়

📌 Intersection (common part) মনে রাখলে সহজ
*/

/*
====================================================
🔹 INNER JOIN = JOIN ?
====================================================

হ্যাঁ ✅

SQL এ:
JOIN == INNER JOIN

দুটো একই জিনিস। মানে  INNER JOIN লেখাও যা হয়, শুধু JOIN লেখলেও একই কাজ হয়।

*/

/*
====================================================
🔹 Multiple Condition দিয়ে INNER JOIN
====================================================
*/

`SELECT *
FROM orders
INNER JOIN users
ON orders.user_id = users.id
AND users.active = true`;

// 👉 একাধিক শর্ত দিয়েও join করা যায়।

/*
====================================================
🔹 INNER JOIN with WHERE
====================================================
*/
`SELECT users.name, orders.product
FROM users
INNER JOIN orders
ON users.id = orders.user_id
WHERE orders.product = 'Laptop'`;

// 👉 আগে JOIN হয়
// 👉 তারপর WHERE filter করে
//
