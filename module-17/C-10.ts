/*
===========================================================
🔥 PostgreSQL — DELETE vs DROP TABLE vs DROP COLUMN
===========================================================

1️⃣ DELETE (Row Delete)
-----------------------
✔ কাজ: Table-এর ভেতরের নির্দিষ্ট row (data) মুছে ফেলে
✔ Table structure অপরিবর্তিত থাকে
✔ Constraints / columns / indexes থাকে
✔ Data ফেরত আনা সম্ভব (transaction ব্যবহার করলে)
✔ Syntax:
    DELETE FROM table_name WHERE condition;
-------------------------------------------
Example:
    DELETE FROM users WHERE id = 5;
-------------------------------------------


2️⃣ DROP TABLE (Full Table Remove)
----------------------------------
✔ কাজ: পুরো table কে সম্পূর্ণ remove করে দেয়
✔ Data + Structure = সব মুছে যায়
✔ Table আর থাকবে না
✔ Undo সম্ভব না (transaction ছাড়া)
✔ Syntax:
    DROP TABLE table_name;
-------------------------------------------
Example:
    DROP TABLE users;
-------------------------------------------


3️⃣ DROP COLUMN (Column Remove)
-------------------------------
✔ কাজ: Table-এর একটি নির্দিষ্ট column মুছে ফেলে
✔ বাকি table structure থাকে
✔ ওই column-এর data স্থায়ীভাবে মুছে যায়
✔ Constraint থাকলে CASCADE প্রয়োজন হতে পারে
✔ Syntax:
    ALTER TABLE table_name DROP COLUMN column_name;

-------------------------------------------
Example:
    ALTER TABLE users DROP COLUMN age;
    // যদি constraint থাকে:
    ALTER TABLE users DROP COLUMN age CASCADE;
-------------------------------------------


===========================================================
🆚 Quick Comparison (Summary)
===========================================================

DELETE:
    → শুধু row delete করে
    → Table structure থাকে

DROP TABLE:
    → পুরো table delete করে
    → Structure + Data দুটোই মুছে যায়

DROP COLUMN:
    → শুধু নির্দিষ্ট column delete করে
    → Table থাকে কিন্তু ওই column আর থাকে না
===========================================================
*/
